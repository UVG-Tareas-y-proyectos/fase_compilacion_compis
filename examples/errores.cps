function sinRetorno(valor: integer): integer {
    if (valor > 0) {
        return valor;
    }
}

const limite: integer = 10;
limite = 20;

let numero: integer = "texto";
let logica: boolean = 2 + true;
noDeclarada = 5;

class Persona {
    let nombre: string;
    function constructor(nombre: string) {
        this.nombre = nombre;
    }
}

let persona: Persona = new Persona(123);
print(persona.edad);

let mezcla = [1, "dos"];
let lista: integer[] = [1, 2, 3];
print(lista[false]);

if (numero) {
    print(numero);
}

break;

